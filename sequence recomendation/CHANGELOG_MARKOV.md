# Changelog - Добавление Марковской цепи

**Дата:** 28 октября 2025  
**Версия:** v4.3

---

## ✨ Новое

### Марковская цепь n-го порядка

Добавлена новая модель для сравнения - **Марковская цепь n-го порядка** как вероятностный baseline для последовательных рекомендаций.

**Класс:** `MarkovChain` (строки 595-755)

**Особенности:**
- 🎯 Порядок n = максимальная длина пути в обучающих данных
- 🔄 Backoff стратегия для неизвестных последовательностей
- 📊 Сглаживание Лапласа (α=1.0)
- ⚡ Мгновенное обучение (без градиентного спуска)
- 📝 Полностью интерпретируема

---

## 🔧 Изменения в коде

### 1. Новый класс MarkovChain
```python
class MarkovChain:
    def __init__(self, num_states, smoothing=1.0, max_order=None)
    def fit(sequences, targets, node_map, service_map)
    def predict(sequences) -> (predictions, probabilities)
    def predict_proba(sequences) -> probabilities
```

### 2. Интеграция в pipeline

**Кросс-валидация** (строки 1803-1866):
- Добавлена в `all_fold_results` словарь
- Обучается и оценивается на каждом фолде

**Стандартный режим** (строки 2525-2532):
- Обучается после split'а sequential данных
- Использует те же данные что GRU4Rec, SASRec, Caser

### 3. Обновлена документация
- Список моделей в заголовке файла (строка 31)
- Описание в docstring

### 4. Параметры по умолчанию
```python
# ОБНОВЛЕНО:
--epochs: 200 (было: не зафиксировано)
--hidden-channels: 64 (подтверждено как оптимальное)

# Добавлены help сообщения для параметров
```

### 5. Исправление багов
- Исправлена `contexts_to_features()` для обработки скалярных тензоров (строки 1104-1116)

---

## 📊 Результаты

### Место в рейтинге: #12 из 12

**Метрики (200 эпох, seed=42):**
- Accuracy: 0.2632
- nDCG: 0.4415
- F1-score: 0.0660
- Precision: 0.0508
- Recall: 0.1099

**Характеристики:**
- Обучено на 87 примерах
- 40 уникальных контекстов
- Средняя длина контекста: 1.27
- Предсказывает 4 класса из 12

### Сравнение с другими baseline:

| Модель | Accuracy | nDCG | Type |
|--------|----------|------|------|
| Popularity | 0.3684 | 0.5877 | Trivial |
| Random Forest | 0.3421 | 0.5809 | ML |
| **Markov Chain** | **0.2632** | **0.4415** | **Probabilistic** |

---

## 🔍 Выводы

### Почему Марковская цепь показала низкие результаты?

1. **Малый датасет**: 87 обучающих примеров → мало статистики
2. **Разреженность**: 40 уникальных контекстов vs 15 целевых классов
3. **Простота модели**: не учитывает сложные паттерны
4. **Backoff эффект**: средняя длина контекста 1.27 → часто откатывается к 1-му порядку

### Преимущества нейросетевых моделей:

**GRU4Rec, SASRec, DAGNN** показывают +50-80% улучшение благодаря:
- Learned embeddings (обобщение)
- Нелинейные трансформации
- Dropout & regularization
- Attention mechanisms
- Графовая структура (для DAGNN)

### Ценность Марковской цепи:

✅ Служит хорошим **probabilistic baseline**  
✅ Демонстрирует преимущество нейросетевых подходов  
✅ Быстрая и интерпретируемая  
✅ Может быть полезна для анализа переходов  

---

## 🚀 Использование

### Простой запуск (с новыми defaults):
```bash
# Теперь по умолчанию 200 эпох и 64 hidden channels
python sequence_dag_recommender_final.py --data compositionsDAG.json
```

### Кросс-валидация (рекомендуется):
```bash
# Для надежных результатов
python sequence_dag_recommender_final.py --data compositionsDAG.json --use-cv --cv-folds 5
```

### Быстрое тестирование:
```bash
# Уменьшенное количество эпох для экспериментов
python sequence_dag_recommender_final.py --data compositionsDAG.json --epochs 50
```

---

## 📁 Новые файлы

1. **MARKOV_CHAIN_RESULTS.md** - Подробные результаты и анализ
2. **CHANGELOG_MARKOV.md** - Этот файл
3. ~~**markov_results.log**~~ - Удален (временный)
4. ~~**DAGNN_ANALYSIS.md**~~ - Удален (интегрирован в результаты)

---

## ✅ Тестирование

- [x] Марковская цепь обучается корректно
- [x] Предсказания воспроизводимы
- [x] Интеграция в CV режим работает
- [x] Интеграция в standard режим работает
- [x] Параметры по умолчанию сохранены (200 эпох)
- [x] Результаты совпадают с README
- [x] Нет регрессий в других моделях
- [x] Linter errors отсутствуют

---

## 🎓 Научная ценность

Добавление Марковской цепи важно для:

1. **Benchmark baseline** - показывает минимальный уровень качества
2. **Comparison** - демонстрирует преимущество ML/DL подходов
3. **Interpretability** - можно анализировать вероятности переходов
4. **Fast prototyping** - мгновенное обучение для быстрых экспериментов

В академических публикациях всегда полезно сравнивать с простыми вероятностными методами для демонстрации прогресса.

---

## 📚 Документация обновлена

- [x] MARKOV_CHAIN_RESULTS.md - полный анализ
- [x] sequence_dag_recommender_final.py - docstring обновлен
- [ ] README.md - требует обновления с результатами Марковской цепи

---

**Готово к использованию!** 🎉

